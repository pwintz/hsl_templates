%%% STEP 0
%%%
%%% Comment/Uncomment items below for either
%%% confeerence or report format.
%%%

% Version 1
%%% FOR REPORT FORMAT
\documentclass[11pt]{article}

\usepackage{pwintz_configuration}
\usepackage{pwintz_definitions}
\input{document_defintions}
\usepackage{demo_setup} % TODO: Delete this.
\graphicspath{{images/}{../images/}}

%%% STEP 1
%%%
%%% Use the "hslTR" package. Make sure you have the
%%% "hslTR.sty" file in your current directory.
%%%
%%%  HSL LAB PACKAGE -- MODIFIED HSL TR PACKAGE - --
\usepackage{./macros/hslTR}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% VERSION CONTROL COMMAND
%%% For Conference, Change to "true"
%%% For Report, Change to "false"
\newbool{conf}
\setbool{conf}{false}
\newcommand{\confonly}[1]{}%\red{#1}}%$\langle$confonly$\rangle$: #1}}
\newcommand{\reportonly}[1]{#1}%{\darkgreen{#1}}

%%% Example of boolean code structure:
\ifbool{conf}{
% Items for conference version go in here.
}{
% Items for report version go in here.
 \addtolength{\oddsidemargin}{-.875in}
	\addtolength{\evensidemargin}{-.875in}
	\addtolength{\textwidth}{1.75in}
	\addtolength{\topmargin}{-.875in}
	\addtolength{\textheight}{1.75in} 
}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%      
% For speed compilation purposes (process figures when needed)
%\includeonly{} % comment this if you want to process the figures (or select which tex file you want to process - one per figure)
% \let\clearpage\relax 


\begin{document}

% IF REPORT
%%%
%%% STEP 2
%%%
%%% Include the following code after the "\begin{document}"
%%% statement. Make sure you set the following:
%%%
%%% \ititle{}  : title of the paper
%%% \iauthor{} : authors of the paper
%%% \iyear{}   : year
%%% \iredno{}  : report number (make sure you get a unique report
%%%             number)
%%%
%%% -- - BEGIN ISG TR - --

% Populate poster title from value defined in document_setup.tex.
\makeatletter
\ititle{\pwintz@title}
\makeatother

\iauthor{
  \csname pwintz@author1\endcsname%
  % Insert second author, if defined.
  \ifcsdef{pwintz@author2}{% If defined
      \\ {\normalsize \csname pwintz@author2\endcsname}%
  }{}%
  % Insert third author, if defined.
  \ifcsdef{pwintz@author3}{% If defined
      \\ {\normalsize \csname pwintz@author3\endcsname}%
  }{}%
  % Insert fourth author, if defined.
  \ifcsdef{pwintz@author4}{% If defined
      \\ {\normalsize \csname pwintz@author4\endcsname}%
  }{}
}
\idate{\today{}} 
\iyear{\red{UPDATE ME}}
\irefnr{\red{UPDATE ME}}
\makeititle

\input{paper_body}

\end{document}

